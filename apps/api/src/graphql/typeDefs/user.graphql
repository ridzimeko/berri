type User {
  id: ID!
  username: String!
  email: String!
  createdAt: 
}

type Query {
  userInfo: User
  users: [User]
}

type Mutation {
  createUser(username: String!, email: String!, password: String!): User
}